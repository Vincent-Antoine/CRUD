<header class="bg-primary text-white p-4 mb-4">
    <div class="container d-flex justify-content-between align-items-center">
        <h1 class="display-4"><i class="bi bi-book"></i> Carnet d'Adresses</h1>
        <nav>
            <ul class="list-inline mb-0">
                <li class="list-inline-item">
                    <a href="{{ path('homepage') }}" class="text-white text-decoration-none">
                        <i class="bi bi-house-door"></i> Accueil
                    </a>
                </li>
                <li class="list-inline-item">
                    <a href="{{ path('contact_new') }}" class="text-white text-decoration-none">
                        <i class="bi bi-plus-circle"></i> Ajouter un contact
                    </a>
                </li>
                <li class="list-inline-item">
                    <a href="{{ path('contact_list') }}" class="text-white text-decoration-none">
                        <i class="bi bi-list"></i> Liste des contacts
                    </a>
                </li>
                {% if app.user %}
                    <li class="list-inline-item">
                        <a href="{{ path('app_logout') }}" class="text-white text-decoration-none">
                            <i class="bi bi-box-arrow-right"></i> Déconnexion
                        </a>
                    </li>
                {% else %}
                    <li class="list-inline-item">
                        <a href="{{ path('app_login') }}" class="text-white text-decoration-none">
                            <i class="bi bi-box-arrow-in-right"></i> Connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </nav>
    </div>
</header>
